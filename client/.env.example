# API Configuration
# For local development, the proxy in vite.config.js will handle /api requests
# For production (Netlify), set this to your backend URL
VITE_API_BASE_URL=http://localhost:5000/api
